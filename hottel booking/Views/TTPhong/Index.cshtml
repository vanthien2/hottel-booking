@model IEnumerable<hottel_booking.Models.TrangThaiPhong>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>TRẠNG THÁI PHÒNG </h2>
<table border="1px">
    <tr>
        <th>MÃ TRẠNG THÁI PHÒNG: </th>
        <th>TRẠNG THÁI: </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.matrangthaiPhong</td>
            <td>@item.trangthai</td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.matrangthaiPhong }) |
                @Html.ActionLink("Chi tiết", "Detail", new { id = item.matrangthaiPhong }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.matrangthaiPhong })
            </td>
        </tr>
    }
</table>
@Html.ActionLink("Thêm mới", "Create", "TTPhong")



